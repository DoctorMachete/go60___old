manifest:
  remotes:
#    - name: moergo-sc
#      url-base: https://github.com/moergo-sc
    - name: darknao
      url-base: https://github.com/darknao
    - name: urob
      url-base: https://github.com/urob             
    - name: dhruvinsh                                # TRI-STATE
      url-base: https://github.com/dhruvinsh
    - name: ssbb                                    # ZMK LISTENERS *** DOESN'T WORK ***
      url-base: https://github.com/ssbb             # ZMK LISTENERS *** DOESN'T WORK ***
    - name: rafaelromao                              # LAYER MORPH
      url-base: https://github.com/rafaelromao             

    - name: badjeff                              # BEHAVIOR LISTENER
      url-base: https://github.com/badjeff


  projects:
  #  - name: zmk                                      # ORIGINAL
  #    remote: moergo-sc
  #    revision: main
  #    import: app/west.yml
    - name: zmk                                       # PER-LAYER RGB
      remote: darknao
      revision: rgb-layer-24.12
      import: app/west.yml

    - name: zmk-unicode
      remote: urob
      revision: main

    - name: zmk-tri-state
      remote: dhruvinsh
      revision: main

    - name: zmk-adaptive-key
      remote: urob
      revision: v0.3 # Should match ZMK release, using v0.3 instead of main just in case it breaks later on

    - name: zmk-leader-key
      remote: urob
      revision: v0.3 # Should match ZMK release, using v0.3 instead of main just in case it breaks later on
      
    - name: zmk-listeners            # *** DOESN'T WORK ***
      remote: ssbb                   # *** DOESN'T WORK ***
      revision: v1                   # *** DOESN'T WORK ***
      
    - name: zmk-layer-morph
      remote: rafaelromao
      revision: v0.3 # set to same as ZMK version above

    - name: zmk-output-behavior-listener
      remote: badjeff
      revision: main
    - name: zmk-drv2605-driver
      remote: badjeff
      revision: main

  self:
    path: config
